# Clerk (Auth & Permissions) — same Clerk project as apps/web, publishable key only
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."

# apps/web dev server, reachable from device/simulator (not "localhost" on a physical device)
EXPO_PUBLIC_API_URL="http://localhost:3000"
