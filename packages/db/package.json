{
  "name": "@rebound/db",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "generate": "prisma generate",
    "push": "prisma db push",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "seed": "tsx scripts/seed-exercises.ts",
    "seed:presets": "tsx scripts/seed-presets.ts",
    "seed:skeletons": "tsx scripts/seed-preset-skeletons.ts",
    "seed:test-fixtures": "tsx scripts/seed-test-fixtures.ts"
  },
  "dependencies": {
    "@prisma/client": "^7.9.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "prisma": "^7.9.1",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
